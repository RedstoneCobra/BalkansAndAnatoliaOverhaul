########################################
#                                      #
#     FormingHistoricalNations.txt     #
#                                      #
########################################
#
# 2011-jan-16 FB add ai_importance = 200
# 2011-aug-01 FB fixes to denmark_norway_nation
# 2012-jul-04 GG new requirement : either 80% of provinces or 66% and capital province
# 2013-aug-26 GG EUIV changes
#
########################################
#
# List of decisions :
#
# prussian_nation
# form_kingdom_of_prussia
# anglofrench_nation
# denmark_norway_nation
# spanish_nation
# english_nation
# french_nation
# form_greece_kingdom
# irish_nation
# mughal_nation
# aztec_empire
# mayan_empire
# incan_empire
# netherlands_nation
# unite_turkish_nation
# unite_turkish_republic
# persian_nation
# roumanian_nation
# russian_nation
# saxon_nation
# nation_bretonne
# british_nation
# bulgaria_nation
# italian_nation_no_hre
# tuscany_nation
# sicilian_nation
# german_nation
# bavarian_nation
# swabian_nation
# hannover_nation
# form_sardinia_piedmont
# form_julich_berg_kleve
# form_siam
# form_goldenhorde
# form_celestial_empire
# westphalia_nation
# malaccan_nation
# polish_nation
# polish_lithuanian_nation
# austrian_nation
# egyptian_nation
# become_sultan_of_delhi
# georgian_nation
# assam_nation
#
# To be added later on :
#
# austro_hungarian_nation
#
########################################

country_decisions = {
	
	prussian_nation = {
		major = yes
		potential = {
			tag = TEU
			NOT = { exists = PRU }
		}
		allow = {
			OR = {
				AND = {
					check_variable = { which = "Demesne_in_Prussia" value = 4 }
					is_subject_other_than_tributary_trigger = yes
				}
				AND = {
					check_variable = { which = "Demesne_in_Prussia" value = 6 }
					check_variable = { which = "Cores_on_Prussia" value = 5 }
					is_free_or_tributary_trigger = yes
				}
			}
			OR = {
				religion = protestant
				religion = reformed
				religion = hussite
				government = monarchy
				government = republic
			}
			owns_or_non_sovereign_subject_of = 41 # Ostpreussen
			is_at_war = no
		}
		effect = {
			prussia_region = { limit = { owned_by = ROOT } remove_core = PRU add_core = PRU }
			if = {
				limit = {
					is_free_or_tributary_trigger = yes
				}
				prussia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PRU }
			}
			if = {
				limit = {
					is_subject_other_than_tributary_trigger = yes
				}
				prussia_region = {
					limit = {
						NOT = { owned_by = ROOT }
					}
					add_permanent_claim = PRU
				}
				overlord = {
					prussia_region = {
						limit = {
							owned_by = PREV
						}
						remove_claim = PRU
					}
				}
			}
			add_prestige = 20
			if = {
				limit = { NOT = { adm_tech = 35 } }
				change_government = feudal_monarchy
			}
			if = {
				limit = { adm_tech = 35 }
				change_government = absolute_monarchy
			}
			if = {
				limit = {
					OR = {
						has_country_modifier = title_1
						has_country_modifier = title_2
					}
				}
				change_title_3 = yes
			}
			change_tag = PRU
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = PRU_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					capital_scope = { region = prussia_region }
					NOT = { tag = ROOT }
				}
				add_historical_rival = PRU
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_kingdom_of_prussia = {
		major = yes
		potential = {
			NOT = { exists = PRU }
			NOT = { exists = GER }
			NOT = { tag = TEU }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			culture_group = low_germanic
			capital_scope = { is_part_of_hre = yes }
			OR = {
				government = monarchy
				government = republic
			}
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_Prussia" value = 10 }
				check_variable = { which = "Cores_on_Prussia" value = 6 }
				AND = {
					check_variable = { which = "Cores_on_Prussia" value = 4 }
					adm_tech = 40
				}
			}
			is_at_war = no
		}
		effect = {
			prussia_region = { limit = { owned_by = ROOT } remove_core = PRU add_core = PRU }
			prussia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PRU }
			add_prestige = 20
			if = {
				limit = { NOT = { adm_tech = 35 } }
				change_government = feudal_monarchy
			}
			if = {
				limit = { adm_tech = 35 }
				change_government = absolute_monarchy
			}
			if = {
				limit = {
					NOT = { has_country_modifier = title_5 }
					NOT = { has_country_modifier = title_6 }
				}
				change_title_5 = yes
			}
			change_tag = PRU
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = PRU_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					capital_scope = { region = prussia_region }
					NOT = { tag = ROOT }
				}
				add_historical_rival = PRU
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	denmark_norway_nation = {
		major = yes
		potential = {
			NOT = { exists = DAN }
			forming_DAN_trigger = yes
			NOT = { tag = KAL }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = ROM }
			OR = {
				tag = DEN
				tag = NOR
			}
		}
		allow = {
			OR = {
				AND = {
					OR = {
						AND = {
							primary_culture = danish
							NOT = { exists = DEN }
						}
						tag = DEN
					}
					NOT = { exists = NOR }
				}
				AND = {
					OR = {
						AND = {
							primary_culture = norwegian
							NOT = { exists = NOR }
						}
						tag = NOR
					}
					NOT = { exists = DEN }
				}
			}
			OR = {
				check_variable = { which = "Demesne_in_Denmark" value = 8 }
				check_variable = { which = "Demesne_in_Norway" value = 5 }
				AND = {
					check_variable = { which = "Cores_on_Denmark" value = 7 }
					check_variable = { which = "Cores_on_Norway" value = 4 }
				}
			}
			is_free_or_tributary_trigger = yes
			owns = 12 # Sjaeland
			owns = 16 # Akerhus
			
			is_at_war = no
		}
		effect = {
			danish_region = { limit = { owned_by = ROOT } remove_core = DAN add_core = DAN }
			danish_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = DAN }
			norwegian_region = { limit = { owned_by = ROOT } remove_core = DAN add_core = DAN }
			norwegian_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = DAN }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = DAN
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = DAN_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					OR = {
						primary_culture = norwegian
						primary_culture = danish
						capital_scope = { region = danish_region }
						capital_scope = { region = norwegian_region }
					}
					NOT = { tag = ROOT }
				}
				add_historical_rival = DAN
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	spanish_nation = {
		major = yes
		potential = {
			NOT = { tag = FRA }
			NOT = { exists = SPA }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			OR = {
				culture_group = iberian
				primary_culture = basque
				primary_culture = catalan
			}
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			NOT = { CAS = { is_subject_of_type = permanent_union } }
			NOT = { ARA = { is_subject_of_type = permanent_union } }
			is_at_war = no
			NOT = { andalucia_region = { owner = { religion_group = muslim } } }
			OR = {
				AND = {
					check_variable = { which = "Demesne_in_Castille" value = 15 }
					check_variable = { which = "Demesne_in_Leon" value = 12 }
					check_variable = { which = "Demesne_in_Aragon" value = 15 }
					check_variable = { which = "Demesne_in_Andalusia" value = 14 }
				}
				AND = {
					check_variable = { which = "Cores_on_Castille" value = 10 }
					check_variable = { which = "Cores_on_Leon" value = 8 }
					check_variable = { which = "Cores_on_Aragon" value = 10 }
					check_variable = { which = "Cores_on_Andalusia" value = 9 }
				}
			}
			NOT = { religion_group = muslim }
		}
		effect = {
			castille_region = { limit = { owned_by = ROOT } remove_core = SPA add_core = SPA }
			castille_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPA }
			leon_region = { limit = { owned_by = ROOT } remove_core = SPA add_core = SPA }
			leon_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPA }
			aragon_region = { limit = { owned_by = ROOT } remove_core = SPA add_core = SPA }
			aragon_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPA }
			andalucia_region = { limit = { owned_by = ROOT } remove_core = SPA add_core = SPA }
			andalucia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPA }
			if = {
				limit = {
					OR = {
						ARA = { vassal_of = ROOT }
						ARA = { junior_union_with = ROOT }
						CAS = { vassal_of = ROOT }
						CAS = { junior_union_with = ROOT }
					}
				}
				add_absolutism = -10
			}
			if = {
				limit = {
					NOT = {
						OR = {
							ARA = { vassal_of = ROOT }
							ARA = { junior_union_with = ROOT }
							CAS = { vassal_of = ROOT }
							CAS = { junior_union_with = ROOT }
						}
					}
				}
				add_absolutism = 10
			}
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						ARA = { vassal_of = ROOT }
						ARA = { junior_union_with = ROOT }
					}
				}
				inherit = ARA
				every_owned_province = {
					limit = {
						OR = {
							culture = aragonese
							culture = catalan
							culture_group = basque_group
						}
						OR = {
							is_core = ARA
							is_core = NAV
						}
					}
					add_local_autonomy = 60
					add_province_modifier = { name = fueros duration = -1 }
				}
			}
			if = {
				limit = {
					OR = {
						CAS = { vassal_of = ROOT }
						CAS = { junior_union_with = ROOT }
					}
				}
				inherit = CAS
				every_owned_province = {
					limit = {
						OR = {
							culture = castillian
							culture = asturian
						}
						is_core = CAS
					}
					add_local_autonomy = 60
					add_province_modifier = { name = fueros duration = -1 }
				}
			}
			change_tag = SPA
			every_country = {
				limit = {
					junior_union_with = ROOT
					capital_scope = {
						OR = {
							region = aragon_region
							region = castille_region
							region = leon_region
							region = andalucia_region
						}
					}
				}
				set_country_flag = junior_union_at_formation
			}
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = SPA_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					OR = {
						capital_scope = { region = aragon_region }
						capital_scope = { region = castille_region }
						capital_scope = { region = leon_region }
						capital_scope = { region = andalucia_region }
					}
					NOT = { tag = ROOT }
				}
				add_historical_rival = SPA
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	english_nation = {
		major = yes
		potential = {
			NOT = { exists = ENG }
			NOT = { exists = GBR }
			forming_ENG_trigger = yes
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			primary_culture = english
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_England" value = 16 }
				check_variable = { which = "Cores_on_England" value = 12 }
			}
			owns = 236		# London
			is_at_war = no
		}
		effect = {
			north_england_region = { limit = { owned_by = ROOT } remove_core = ENG add_core = ENG }
			north_england_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ENG }
			south_england_region = { limit = { owned_by = ROOT } remove_core = ENG add_core = ENG }
			south_england_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ENG }
			add_prestige = 20
			ENG = { set_capital = 236 }
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = ENG
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = ENG_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					OR = {
						primary_culture = english
						capital_scope = { region = north_england_region }
						capital_scope = { region = south_england_region }
					}
					NOT = { tag = ROOT }
				}
				add_historical_rival = ENG
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	french_nation = {
		major = yes
		potential = {
			NOT = { tag = SPA }		# Basque is a valid culture for both FRA and SPA
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			NOT = { exists = FRA }
			forming_FRA_trigger = yes
			NOT = { tag = ENG }
			OR = {
				culture_group = langue_d_oil
				culture_group = langue_d_oc
				tag = NAV
				#culture_group = basque
				#primary_culture = breton
			}
			is_colonial_nation = no
			#num_of_cities = 6
		}
		allow = {
			is_free_or_tributary_trigger = yes
			OR = {
				has_country_flag = true_france
				AND = {
					owns = 177 # Maine
					owns = 183 # Paris
					owns = 184 # Orliens
					owns = 186 # Argonne
					owns = 1335 # Brye
					owns = 1386 # Perche
					owns = 1387 # Valoys
					owns = 2330 # Evrecin
				}
			}
			OR = {
				check_variable = { which = "Demesne_in_France" value = 30 }
				check_variable = { which = "Cores_on_France" value = 23 }
			}
			is_at_war = no
		}
		effect = {
			france_superregion = { limit = { owned_by = ROOT } add_core = FRA }
			france_superregion = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = FRA }
			add_prestige = 20
			FRA = { set_capital = 183 } # Paris
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = FRA
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = FRA_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					OR = {
						culture_group = langue_d_oc
						culture_group = langue_d_oil
						capital_scope = { region = east_france_region }
					}
					NOT = { tag = ROOT }
				}
				add_historical_rival = FRA
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_greece_kingdom = {
		major = yes
		potential = {
			NOT = { exists = GRE }
			OR = {
				primary_culture = greek
				tag = ATH
				tag = ACH
				tag = MOE
				tag = EPI
			}
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = BYZ }
			NOT = { tag = OTT }
			NOT = { tag = TUR }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			owns = 146 # Attica
			is_core = 146 # Attica
			is_at_war = no
			OR = {
				check_variable = { which = "Demesne_in_Greece" value = 20 }
				check_variable = { which = "Cores_on_Greece" value = 16 }
			}
		}
		effect = {
			greece_region = { limit = { owned_by = ROOT } remove_core = GRE add_core = GRE }
			greece_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GRE }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = GRE
			change_primary_culture = greek
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = GRE_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					OR = {
						primary_culture = greek
						capital_scope = { region = greece_region }
					}
					NOT = { tag = ROOT }
				}
				add_historical_rival = GRE
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	irish_nation = {
		major = yes
		potential = {
			OR = {
				primary_culture = irish
				primary_culture = hiberno_norman
			}
			NOT = { exists = IRE }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			NOT = { tag = GBR }
			forming_IRE_trigger = yes
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			check_variable = { which = "Demesne_in_Ireland" value = 15 }
			NOT = { government = irish_monarchy }
		}
		effect = {
			ireland_region = { limit = { owned_by = ROOT } remove_core = IRE add_core = IRE }
			ireland_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = IRE }
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					NOT = {
						has_country_modifier = title_5
						has_country_modifier = title_6
					}
				}
				change_title_5 = yes
			}
			change_tag = IRE
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = irish_ideas }
				}
				swap_national_ideas_effect = yes
			}
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	mughal_nation = {
		major = yes
		potential = {
			NOT = { exists = MUG }
			NOT = { tag = PER }
			NOT = { tag = TUR }
			religion_group = muslim
			OR = {
				culture_group = altaic
				culture_group = tartar_group
				culture_group = persian_group
			}
			OR = {
				owns_or_non_sovereign_subject_of = 521	# Panipat
				owns_or_non_sovereign_subject_of = 522	# Jindh
				owns_or_non_sovereign_subject_of = 524	# Braj
				owns_or_non_sovereign_subject_of = 2163	# Delhi
				owns_or_non_sovereign_subject_of = 2555	# Katehar
				owns_or_non_sovereign_subject_of = 2705	# Mewat
			}
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			owns_or_non_sovereign_subject_of = 521		# Panipat
			owns_or_non_sovereign_subject_of = 522		# Jindh
			owns_or_non_sovereign_subject_of = 524		# Braj
			owns_or_non_sovereign_subject_of = 2163		# Delhi
			owns_or_non_sovereign_subject_of = 2555		# Katehar
			owns_or_non_sovereign_subject_of = 2705		# Mewat
			OR = {
				check_variable = { which = "Cores_on_India" value = 20 }
				check_variable = { which = "Demesne_in_India" value = 30 }
			}
		}
		effect = {
			west_bengal_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			west_bengal_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			east_bengal_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			east_bengal_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			upper_doab_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			upper_doab_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			central_doab_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			central_doab_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			punjab_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			punjab_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			awadh_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			awadh_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			bihar_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			bihar_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			indus_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			indus_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			rajputana_region = { limit = { owned_by = ROOT } remove_core = MUG add_core = MUG }
			rajputana_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					NOT = { has_country_modifier = title_6 }
				}
				change_title_6 = yes
			}
			if = {
				limit = {
					technology_group = steppestech
				}
				change_technology_group = muslim
				#change_unit_type = indian
			}
			change_primary_culture = urdu
			set_country_flag = urdu_state
			change_tag = MUG
			change_government = indian_monarchy
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = MUG_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					OR = {
						capital_scope = { region = east_bengal_region }
						capital_scope = { region = west_bengal_region }
						capital_scope = { region = punjab_region }
						capital_scope = { region = central_doab_region }
						capital_scope = { region = upper_doab_region }
						capital_scope = { region = awadh_region }
						capital_scope = { region = rajputana_region }
					}
					NOT = { tag = ROOT }
				}
				add_historical_rival = MUG
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	aztec_empire = {
		major = yes
		potential = {
			NOT = { exists = AZT }
			primary_culture = nahuatl_c
			is_colonial_nation = no
		}
		allow = {
			OR = {
				check_variable = { which = "Demesne_in_Nahualt" value = 20 }
				check_variable = { which = "Cores_on_Nahualt" value = 15 }
			}
			is_free_or_tributary_trigger = yes
			owns_or_non_sovereign_subject_of = 2200		# Acolhua
			owns_or_non_sovereign_subject_of = 852		# Tepanec
			owns_or_non_sovereign_subject_of = 2406		# Tlacopan
			owns_or_non_sovereign_subject_of = 1471		# Tenochtitlan
			owns_or_non_sovereign_subject_of = 851		# Tlapanec
			owns_or_non_sovereign_subject_of = 2204		# Teotitlan
			owns_or_non_sovereign_subject_of = 2201		# Tlaxcala
			is_at_war = no
		}
		effect = {
			mexico_region = { limit = { owned_by = ROOT } remove_core = AZT add_core = AZT }
			mexico_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = AZT }
			add_prestige = 20
			if = {
				limit = {
					OR = {
						government = tribal_confederation
						government = tribal_federation
					}
				}
				change_government = tribal_monarchy
			}
			if = {
				limit = { NOT = { has_country_modifier = title_6 } }
				change_title_6 = yes
			}
			change_tag = AZT
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = AZT_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					capital_scope = { region = mexico_region }
					NOT = { tag = ROOT }
				}
				add_historical_rival = AZT
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	mayan_empire = {
		major = yes
		potential = {
			NOT = { exists = MAY }
			culture_group = mayan_group
			is_colonial_nation = no
		}
		allow = {
			OR = {
				check_variable = { which = "Demesne_in_Maya" value = 15 }
				check_variable = { which = "Cores_on_Maya" value = 10 }
			}
			is_free_or_tributary_trigger = yes
			owns = 840		# Copan
			is_core = 840
			owns = 845		# Uxmal
			is_core = 845
			owns = 846		# Chichen-Itza
			is_core = 846
			owns = 2208		# Tikal
			is_core = 2208
			is_at_war = no
		}
		effect = {
			guatemala_area = { limit = { owned_by = ROOT } remove_core = MAY add_core = MAY }
			guatemala_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
			yucatan_area = { limit = { owned_by = ROOT } remove_core = MAY add_core = MAY }
			yucatan_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
			chiapas_area = { limit = { owned_by = ROOT } remove_core = MAY add_core = MAY }
			chiapas_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
			belize_area = { limit = { owned_by = ROOT } remove_core = MAY add_core = MAY }
			belize_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
			campeche_area = { limit = { owned_by = ROOT } remove_core = MAY add_core = MAY }
			campeche_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
			add_prestige = 20
			if = {
				limit = {
					OR = {
						government = tribal_confederation
						government = tribal_federation
					}
				}
				change_government = tribal_monarchy
			}
			if = {
				limit = { NOT = { has_country_modifier = title_6 } }
				change_title_6 = yes
			}
			change_tag = MAY
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = MAY_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	incan_empire = {
		major = yes
		potential = {
			NOT = { exists = INC }
			religion = inti
			is_colonial_nation = no
		}
		allow = {
			OR = {
				check_variable = { which = "Demesne_in_Inca" value = 30 }
				check_variable = { which = "Cores_on_Inca" value = 20 }
			}
			is_free_or_tributary_trigger = yes
			owns_core_province = 2068			# Cuzco
			OR = {
				owns_core_province = 816		# Pachacamac
				AND = {
					816 = { owned_by = CHC }
					alliance_with = CHC
				}
			}
			OR = {
				owns_core_province = 820		# Huancayo
				owns_core_province = 3435		# lupaca
			}
			OR = {
				owns_core_province = 811		# Kasha Marka
				owns_core_province = 3436		# Golla
			}
			is_at_war = no
		}
		effect = {
			peru_region = { limit = { owned_by = ROOT } remove_core = INC add_core = INC }
			peru_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = INC }
			add_prestige = 20
			INC = { set_capital = 2068 }
			if = {
				limit = {
					OR = {
						government = tribal_monarchy
						government = tribal_theocracy
						government = tribal_confederation
						government = tribal_federation
					}
				}
				change_government = despotic_monarchy
			}
			if = {
				limit = { NOT = { has_country_modifier = title_6 } }
				change_title_6 = yes
			}
			
			clr_country_flag = great_predator_plan_set
			
			every_province = {
				limit = {
					has_province_flag = conquest_expansion_target_CZC
				}
				clr_province_flag = conquest_expansion_target_CZC
				set_province_flag = conquest_expansion_target_INC
			}
			
			change_tag = INC
			add_absolutism = 10
			if = {
				limit = {
					AND = {
						816 = { owned_by = CHC }
						alliance_with = CHC
					}
				}
				inherit = CHC
			}
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = INC_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	netherlands_nation = {
		major = yes
		potential = {
			NOT = { exists = NED }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = EBU }
			NOT = { tag = LOT }
			NOT = { tag = ROM }
			culture_group = low_frankish
			# NOT = { primary_culture = burgundian }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_the_Netherlands" value = 15 }
				check_variable = { which = "Cores_on_the_Netherlands" value = 10 }
			}
			OR = {
				adm_tech = 25
				ADM = 5
				AND = {
					full_idea_group = mercantilism_ideas
					full_idea_group = humanist_ideas
					NOT = { absolutism = 70 }
					stability = 1
				}
			}
			is_at_war = no
		}
		effect = {
			belgii_region = { limit = { owned_by = ROOT } remove_core = NED add_core = NED }
			belgii_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = NED }
			low_countries_region = { limit = { owned_by = ROOT } remove_core = NED add_core = NED }
			low_countries_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = NED }
			if = {
				limit = {
					government = theocracy
					religion = catholic
				}
				change_government = administrative_monarchy
				PAP = {
					add_opinion = {
						who = ROOT
						modifier = opinion_displeased
					}
				}
			}
			if = {
				limit = {
					government = theocracy
					NOT = { religion = catholic }
				}
				change_government = administrative_monarchy
			}
			if = {
				limit = {
					NOT = {
						has_country_modifier = title_3
						has_country_modifier = title_4
						has_country_modifier = title_5
						has_country_modifier = title_6
					}
				}
				change_title_3 = yes
			}
			add_prestige = 20
			change_tag = NED
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = NED_ideas }
				}
				swap_national_ideas_effect = yes
			}
			# country_event = { id = holyromanempire.5 days = 5 } # Netherlands now have to wait for the Treaty of Westphalia
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	persian_nation = {
		major = yes
		potential = {
			NOT = { exists = PER }
			NOT = { tag = MUG }
			culture_group = persian_group
			OR = {
				owns = 390 # Yazd
				owns = 414 # Isfahan
				#owns = 426 # Mazandaran
				owns = 1313 # Rey
				owns = 1314 # Qasvin
				owns = 2225 # Qom
			}
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_Persia" value = 30 }
				check_variable = { which = "Cores_on_Persia" value = 20 }
			}
			owns = 390 # Yazd
			owns = 414 # Isfahan
			owns = 426 # Mazandaran
			owns = 1313 # Rey
			owns = 1314 # Qasvin
			owns = 2225 # Qom
			is_at_war = no
		}
		effect = {
			persia_region = { limit = { owned_by = ROOT } remove_core = PER add_core = PER }
			persia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PER }
			khorasan_region = { limit = { owned_by = ROOT } remove_core = PER add_core = PER }
			khorasan_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PER }
			afghanistan_region = { limit = { owned_by = ROOT } remove_core = PER add_core = PER }
			afghanistan_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PER }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_6 } }
				change_title_6 = yes
			}
			change_tag = PER
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = PER_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	roumanian_nation = {
		major = yes
		potential = {
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			NOT = { tag = BYZ }
			culture_group = romanian
			NOT = { exists = ROU }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			OR = {
				adm_tech = 35
				AND = {
					ADM = 5
					statesman = 2
				}
				AND = {
					NOT = { absolutism = 80 }
					full_idea_group = administrative_ideas
					full_idea_group = diplomatic_ideas
				}
			}
			OR = {
				check_variable = { which = "Demesne_in_Roumania" value = 12 }
				check_variable = { which = "Cores_on_Roumania" value = 10 }
			}
		}
		effect = {
			south_carpathia_region = { limit = { owned_by = ROOT } remove_core = ROU add_core = ROU }
			south_carpathia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ROU }
			add_prestige = 15
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						WAL = { vassal_of = ROOT }
						WAL = { junior_union_with = ROOT }
					}
				}
				inherit = WAL
			}
			if = {
				limit = {
					OR = {
						MOL = { vassal_of = ROOT }
						MOL = { junior_union_with = ROOT }
					}
				}
				inherit = MOL
			}
			change_tag = ROU
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = RMN_ideas }
				}
				swap_national_ideas_effect = yes
			}
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	russian_nation = {
		major = yes
		potential = {
			NOT = { exists = RUS }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = ROM }
			#government = monarchy
			OR = {
				primary_culture = russian
				primary_culture = novgorodian
				primary_culture = ruthenian
			}
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			absolutism = 25
			OR = {
				adm_tech = 23
				full_idea_group = expansion_ideas
				AND = {
					adm_tech = 20
					ADM = 5
				}
			}
			OR = {
				check_variable = { which = "Demesne_in_Russia" value = 45 }
				check_variable = { which = "Cores_on_Russia" value = 30 }
			}
		}
		effect = {
			north_russia_region = { limit = { owned_by = ROOT } remove_core = RUS add_core = RUS }
			north_russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			russia_region = { limit = { owned_by = ROOT } remove_core = RUS add_core = RUS }
			russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			east_russia_region = { limit = { owned_by = ROOT } remove_core = RUS add_core = RUS }
			east_russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			steppes_region = { limit = { owned_by = ROOT } remove_core = RUS add_core = RUS }
			steppes_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			sloboda_ukraine_area = { limit = { owned_by = ROOT } remove_core = RUS add_core = RUS }
			sloboda_ukraine_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_6 } }
				change_title_6 = yes
			}
			change_tag = RUS
			set_country_flag = rus_formed
			add_absolutism = 10
			
			clr_country_flag = great_predator_plan_set
			
			every_province = {
				limit = {
					has_province_flag = conquest_expansion_target_MOS
				}
				clr_province_flag = conquest_expansion_target_MOS
				set_province_flag = conquest_expansion_target_MOS
			}
			
			set_country_flag = great_predator
			
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = RUS_ideas }
				}
				swap_national_ideas_effect = yes
			}
			every_country = {
				limit = {
					OR = {
						primary_culture = russian
						capital_scope = { region = north_russia_region }
						capital_scope = { region = russia_region }
						capital_scope = { region = east_russia_region }
						tag = LIT
						tag = POL
						tag = PLC
					}
					NOT = { tag = ROOT }
					NOT = { is_subject_of = ROOT }
				}
				add_historical_rival = RUS
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	russian_republic = {
		major = yes
		potential = {
			NOT = { exists = RUS }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			government = republic
			OR = {
				primary_culture = russian
				primary_culture = novgorodian
				primary_culture = ruthenian
			}
			is_colonial_nation = no
			NOT = { has_country_flag = rus_formed }
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			OR = {
				adm_tech = 23
				full_idea_group = expansion_ideas
				AND = {
					adm_tech = 20
					ADM = 5
				}
			}
			OR = {
				check_variable = { which = "Demesne_in_Russia" value = 45 }
				check_variable = { which = "Cores_on_Russia" value = 30 }
			}
		}
		effect = {
			north_russia_region = { limit = { owned_by = ROOT } remove_core = RUS add_core = RUS }
			north_russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			russia_region = { limit = { owned_by = ROOT } remove_core = RUS add_core = RUS }
			russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			east_russia_region = { limit = { owned_by = ROOT } remove_core = RUS add_core = RUS }
			east_russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			add_prestige = 20
			if = {
				limit = { NOT = { has_country_modifier = title_6 } }
				change_title_6 = yes
			}
			set_country_flag = rus_formed
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = RUS_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	saxon_nation = {
		major = yes
		potential = {
			NOT = { exists = SAX }
			OR = {
				primary_culture = high_saxon
				primary_culture = thuringian
			}
			forming_SAX_trigger = yes
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			check_variable = { which = "Demesne_in_the_Upper_Saxon_Circuit" value = 5 }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			check_variable = { which = "Cores_on_the_Upper_Saxon_Circuit" value = 10 }
			OR = {
				AND = {
					owns = 59		# Meissen
					is_core = 59	# Meissen
				}
				AND = {
					owns = 1373		# Leipzig
					is_core = 1373	# Leipzig
				}
				AND = {
					owns = 2617		# Dresden
					is_core = 2617	# Dresden
				}
			}
		}
		effect = {
			upper_saxon_circle_region = { limit = { owned_by = ROOT } remove_core = SAX add_core = SAX }
			upper_saxon_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SAX }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			if = {
				limit = { NOT = { hre_size = 1 } government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = SAX
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = SAX_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	nation_bretonne = {
		major = yes
		potential = {
			NOT = { exists = BRI }
			OR = {
				tag = MNF
				tag = BLO
			}
			forming_BRI_trigger = yes
			check_variable = { which = "Demesne_in_Brittany" value = 3 }
			is_colonial_nation = no
		}
		allow = {
			OR = {
				AND = {
					tag = MNF
					NOT = { exists = BLO }
				}
				AND = {
					tag = BLO
					NOT = { exists = MNF }
				}
			}
			is_free_or_tributary_trigger = yes
			is_at_war = no
			check_variable = { which = "Cores_on_Brittany" value = 4 }
		}
		effect = {
			brittany_area = { limit = { owned_by = ROOT } remove_core = BRI add_core = BRI }
			brittany_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BRI }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			change_tag = BRI
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = BRI_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	british_nation = {
		major = yes
		potential = {
			NOT = { exists = GBR }
			OR = {
				AND = {
					tag = ENG
					NOT = { has_global_flag = hundred_year_war }
					NOT = { has_global_flag = war_of_the_roses }
				}
				NOT = { exists = ENG }
				ENG = {
					is_subject_of = ROOT
				}
			}
			OR = {
				tag = SCO
				NOT = {
					exists = SCO
				}
				SCO = {
					is_subject_of = ROOT
				}
			}
			forming_GBR_trigger = yes
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			OR = {
				culture_group = british
				culture_group = gaelic
			}
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			OR = {
				adm_tech = 35
				AND = {
					absolutism = 70
					adm_tech = 30
					ADM = 5
					south_england_region = {
						type = all
						owned_by = ROOT
					}
					north_england_region = {
						type = all
						owned_by = ROOT
					}
					scotland_region = {
						type = all
						owned_by = ROOT
					}
					ireland_region = {
						type = all
						owned_by = ROOT
					}
				}
			}
			owns = 236	# London
			OR = {
				check_variable = { which = "Demesne_in_Great_Britain" value = 38 }
				AND = {
					check_variable = { which = "Cores_on_England" value = 14 }
					check_variable = { which = "Cores_on_Wales" value = 2 }
					check_variable = { which = "Cores_on_Scotland" value = 8 }
					check_variable = { which = "Cores_on_Ireland" value = 8 }
				}
			}
		}
		effect = {
			south_england_region = { limit = { owned_by = ROOT } remove_core = GBR add_core = GBR }
			south_england_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GBR }
			north_england_region = { limit = { owned_by = ROOT } remove_core = GBR add_core = GBR }
			north_england_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GBR }
			scotland_region = { limit = { owned_by = ROOT } remove_core = GBR add_core = GBR }
			scotland_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GBR }
			ireland_region = { limit = { owned_by = ROOT } remove_core = GBR add_core = GBR }
			ireland_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GBR }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = GBR
			GBR = { set_capital = 236 }
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = ENG_ideas }
				}
				swap_national_ideas_effect = yes
			}
			if = {
				limit = {
					exists = SCO
					NOT = {
						tag = SCO
					}
				}
				inherit = SCO
			}
			if = {
				limit = {
					exists = ENG
					NOT = {
						tag = ENG
					}
				}
				inherit = ENG
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	bulgaria_nation = {
		major = yes
		potential = {
			NOT = { exists = BUL }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = BYZ }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			primary_culture = bulgarian
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			owns = 148	# Ohrid
			owns = 150	# Silistria
			owns = 156	# Nisava
			owns = 157	# Lodogoria
			owns = 159	# Dobrogea
			owns = 1426	# Serdica
			owns = 1427	# Maritsa
			owns = 2381	# Karvuna
			owns = 2382	# Vidin
		}
		effect = {
			east_bulgaria_area = { limit = { owned_by = ROOT } remove_core = BUL add_core = BUL }
			east_bulgaria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BUL }
			west_bulgaria_area = { limit = { owned_by = ROOT } remove_core = BUL add_core = BUL }
			west_bulgaria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BUL }
			dobrogea_area = { limit = { owned_by = ROOT } remove_core = BUL add_core = BUL }
			dobrogea_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BUL }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = BUL
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = BUL_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	serbia_nation = {
		major = yes
		potential = {
			NOT = { exists = SER }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = BYZ }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			primary_culture = serbian
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			# owns = 2612	# Belgrade
			OR = {
				check_variable = { which = "Demesne_in_Serbia" value = 8 }
				check_variable = { which = "Cores_on_Serbia" value = 6 }
			}
		}
		effect = {
			south_serbia_area = { limit = { owned_by = ROOT } remove_core = SER add_core = SER }
			south_serbia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SER }
			north_serbia_area = { limit = { owned_by = ROOT } remove_core = SER add_core = SER }
			north_serbia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SER }
			macedonia_area = { limit = { owned_by = ROOT } remove_core = SER add_core = SER }
			macedonia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SER }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = SER
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = SER_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	italian_nation_no_hre = {
		major = yes
		potential = {
			NOT = { has_global_flag = iron_crown_relinquished }
			NOT = { exists = ITA }
			NOT = { exists = ITE }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			OR = {
				culture_group = latin
				tag = URB
			}
			OR = {
				NOT = { hre_size = 1 }
				has_global_flag = westphalia_italy
				is_emperor = yes
			}
			is_colonial_nation = no
			NOT = { adm_tech = 50 }
		}
		allow = {
			NOT = { has_global_flag = iron_crown_relinquished }
			NOT = { adm_tech = 50 }
			OR = {
				check_variable = { which = "Demesne_in_Italy" value = 30 }
				AND = {
					check_variable = { which = "Cores_on_Emilia_Romagna" value = 4 }
					check_variable = { which = "Cores_on_Tuscany" value = 8 }
					check_variable = { which = "Cores_on_Lombardy" value = 4 }
					check_variable = { which = "Cores_on_Sardinia" value = 2 }
					check_variable = { which = "Cores_on_Liguria_Piedmont" value = 4 }
					check_variable = { which = "Cores_on_Veneto" value = 5 }
					check_variable = { which = "Cores_on_Umbria_Marcha" value = 4 }
				}
			}
			owns = 1347		# Pavia, historical capital
			is_free_or_tributary_trigger = yes
			is_at_war = no
		}
		effect = {
			set_global_flag = iron_crown_relinquished
			east_italy_region = { limit = { owned_by = ROOT } remove_core = ITA add_core = ITA }
			east_italy_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ITA }
			west_italy_region = { limit = { owned_by = ROOT } remove_core = ITA add_core = ITA }
			west_italy_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ITA }
			central_italy_region = { limit = { owned_by = ROOT } remove_core = ITA add_core = ITA }
			central_italy_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ITA }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = ITA
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = ITA_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	italian_nation_in_hre = {
		major = yes
		potential = {
			NOT = { exists = ITA }
			NOT = { exists = ITE }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			OR = {
				culture_group = latin
				tag = URB
			}
			hre_size = 1
			NOT = { is_emperor = yes }
			is_colonial_nation = no
		}
		allow = {
			government = monarchy
			OR = {
				check_variable = { which = "Demesne_in_Italy" value = 30 }
				AND = {
					check_variable = { which = "Cores_on_Emilia_Romagna" value = 4 }
					check_variable = { which = "Cores_on_Tuscany" value = 8 }
					check_variable = { which = "Cores_on_Lombardy" value = 4 }
					check_variable = { which = "Cores_on_Sardinia" value = 2 }
					check_variable = { which = "Cores_on_Liguria_Piedmont" value = 4 }
					check_variable = { which = "Cores_on_Veneto" value = 5 }
					check_variable = { which = "Cores_on_Umbria_Marcha" value = 4 }
				}
			}
			owns = 1347		# Pavia, historical capital
			is_free_or_tributary_trigger = yes
			is_at_war = no
		}
		effect = {
			emperor = {
				country_event = { id = "flavor_ita.1" days = 1 }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	italian_nation_late = {
		major = yes
		potential = {
			NOT = { exists = ITE }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			OR = {
				tag = ITA
				AND = {
					NOT = { exists = ITA }
					OR = {
						culture_group = latin
						tag = URB
					}
				}
			}
			NOT = { hre_size = 1 }
			is_colonial_nation = no
			adm_tech = 50
		}
		allow = {
			adm_tech = 50
			OR = {
				check_variable = { which = "Demesne_in_Italy" value = 30 }
				AND = {
					check_variable = { which = "Cores_on_Emilia_Romagna" value = 4 }
					check_variable = { which = "Cores_on_Tuscany" value = 8 }
					check_variable = { which = "Cores_on_Lombardy" value = 4 }
					check_variable = { which = "Cores_on_Sardinia" value = 2 }
					check_variable = { which = "Cores_on_Liguria_Piedmont" value = 4 }
					check_variable = { which = "Cores_on_Veneto" value = 5 }
					check_variable = { which = "Cores_on_Umbria_Marcha" value = 4 }
				}
			}
			OR = {
				check_variable = { which = "Demesne_in_Two_Sicilies" value = 15 }
				check_variable = { which = "Cores_on_Two_Sicilies" value = 10 }
			}
			OR = {
				owns = 118 # Lazio
				owns = 2530 # Roma
			}
			is_free_or_tributary_trigger = yes
			is_at_war = no
		}
		effect = {
			italy_superregion = { limit = { owned_by = ROOT } remove_core = ITE add_core = ITE }
			italy_superregion = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ITE }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = ITE
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = ITA_ideas }
				}
				swap_national_ideas_effect = yes
			}
			# country_event = { id = holyromanempire.1 days = 5 } # Useless... the Empire has to be gone for the decision to work.
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	tuscany_nation = {
		major = yes
		potential = {
			NOT = { exists = TUS }
			NOT = { tag = ITA }
			NOT = { tag = ITC }
			NOT = { tag = ITD }
			NOT = { tag = ITE }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = SPI }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			primary_culture = tuscan
			NOT = { is_emperor = yes }
			capital_scope = { area = tuscany_area }
			check_variable = { which = "Demesne_in_Tuscany" value = 4 }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			prestige = 25
			OR = {
				adm_tech = 20
				AND = {
					NOT = { absolutism = 90 }
					stability = 1
					full_idea_group = culture_ideas
				}
				AND = {
					ADM = 5
					statesman = 2
				}
			}
			government = monarchy
			OR = {
				check_variable = { which = "Demesne_in_Tuscany" value = 8 }
				check_variable = { which = "Cores_on_Tuscany" value = 6 }
			}
			is_at_war = no
#			every_country = {
#				limit = { government = papal_government }
#				add_opinion = { who = ROOT modifier = opinion_improved_relations }
#			}
		}
		effect = {
			tuscany_area = { limit = { owned_by = ROOT } remove_core = TUS add_core = TUS }
			tuscany_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = TUS }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_4 has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_4 = yes
			}
			change_tag = TUS
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = TUS_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	sicilian_nation = {
		major = yes
		potential = {
			NOT = { exists = NAP }
			OR = { #There can only be one Sicily alive
				AND = {
					tag = SIC
					NOT = { exists = KNP }
				}
				AND = {
					tag = KNP
					NOT = { exists = SIC }
				}
			}
			NOT = { is_emperor = yes }
			is_colonial_nation = no
			adm_tech = 20
			capital_scope = { region = sicily_region }
			culture_group = napolitan
		}
		allow = {
			government = monarchy
			prestige = 50
			is_free_or_tributary_trigger = yes
			OR = {
				adm_tech = 30
				AND = {
					NOT = { absolutism = 90 }
					stability = 3
					full_idea_group = administrative_ideas
					full_idea_group = culture_ideas
					full_idea_group = diplomatic_ideas
				}
				AND = {
					ADM = 6
					statesman = 1
				}
			}
			owns = 2531	# Napoli
			owns = 125	# Palermu
			OR = {
				check_variable = { which = "Demesne_in_Two_Sicilies" value = 20 }
				check_variable = { which = "Cores_on_Two_Sicilies" value = 15 }
			}
			is_at_war = no
		}
		effect = {
			sicily_region = { limit = { owned_by = ROOT } remove_core = NAP add_core = NAP }
			sicily_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = NAP }
			naples_region = { limit = { owned_by = ROOT } add_core = NAP }
			naples_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = NAP }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = NAP
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = NAP_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	german_nation = {
		major = yes
		potential = {
			NOT = { exists = GER }
			OR = {
				NOT = { tag = ERG }
				AND = {
					dip_tech = 45
					NOT = {
						tag = ERG
						ai = yes
					}
				}
			}
			NOT = { tag = PAP }
			OR = {
				culture_group = high_germanic
				culture_group = low_germanic
			}
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_Germany" value = 70 }
				AND = {
					check_variable = { which = "Cores_on_Bavarian_Circuit" value = 3 }
					check_variable = { which = "Cores_on_Franconian_Circuit" value = 3 }
					check_variable = { which = "Cores_on_the_Swabian_Circuit" value = 6 }
					check_variable = { which = "Cores_on_the_Upper_Saxon_Circuit" value = 10 }
					check_variable = { which = "Cores_on_the_Lower_Saxon_Circuit" value = 10 }
					check_variable = { which = "Cores_on_the_Upper_Rhine_Circuit" value = 9 }
					check_variable = { which = "Cores_on_Westphalia" value = 4 }
				}
			}
			is_at_war = no
			OR = {
				NOT = { hre_size = 1 }
				AND = {
					hre_size = 1
					NOT = { hre_size = 2 }
					is_emperor = yes
				}
				adm_tech = 50
			}
		}
		effect = {
			lower_saxon_circle_region = { limit = { owned_by = ROOT } remove_core = GER add_core = GER }
			lower_saxon_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			upper_saxon_circle_region = { limit = { owned_by = ROOT } remove_core = GER add_core = GER }
			upper_saxon_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			westphalian_circle_region = { limit = { owned_by = ROOT } remove_core = GER add_core = GER }
			westphalian_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			upper_rhenish_circle_region = { limit = { owned_by = ROOT } remove_core = GER add_core = GER }
			upper_rhenish_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			swabia_bavarian_region = { limit = { owned_by = ROOT } remove_core = GER add_core = GER }
			swabia_bavarian_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			#all_core_province = { limit = { owned_by = ROOT } add_core = GER }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = GER
			add_absolutism = 10
			emperor = {
				add_opinion = {
					who = root
					modifier = opinion_left_empire
				}
			}
			set_in_empire = no
			emperor = {
				country_event = { id = hre_event.5 }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	bavarian_nation = {
		major = yes
		potential = {
			NOT = { exists = BAV }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			primary_culture = bavarian
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			check_variable = { which = "Demesne_in_Bavarian_Circuit" value = 8 }
			is_core = 64
			is_core = 65
			is_core = 1351
			is_core = 1509
			is_at_war = no
		}
		effect = {
			lower_bavaria_area = { limit = { owned_by = ROOT } remove_core = BAV add_core = BAV }
			lower_bavaria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BAV }
			upper_bavaria_area = { limit = { owned_by = ROOT } remove_core = BAV add_core = BAV }
			upper_bavaria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BAV }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			if = {
				limit = { NOT = { hre_size = 1 } government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = BAV
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = BAV_ideas }
				}
				swap_national_ideas_effect = yes
			}
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	swabian_nation = {
		major = yes
		potential = {
			NOT = { exists = SWA }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			primary_culture = schwabisch
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			check_variable = { which = "Demesne_in_the_Swabian_Circuit" value = 8 }
			is_core = 77
			
			is_at_war = no
		}
		effect = {
			lower_swabia_area = { limit = { owned_by = ROOT } remove_core = SWA add_core = SWA }
			lower_swabia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SWA }
			upper_swabia_area = { limit = { owned_by = ROOT } remove_core = SWA add_core = SWA }
			upper_swabia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SWA }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			if = {
				limit = { NOT = { hre_size = 1 } government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = SWA
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	lower_saxony_nation = {
		major = yes
		potential = {
			NOT = { exists = LSA }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			OR = {
				primary_culture = old_saxon
				primary_culture = eastphalian
				primary_culture = westphalian
			}
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			OR = {
				check_variable = { which = "Demesne_in_Eastphalia" value = 8 }
				check_variable = { which = "Cores_on_Eastphalia" value = 5 }
			}
			OR = {
				check_variable = { which = "Demesne_in_Westphalia" value = 8 }
				check_variable = { which = "Cores_on_Westphalia" value = 5 }
			}
		}
		effect = {
			westphalian_circle_region = { limit = { owned_by = ROOT } remove_core = LSA add_core = LSA }
			westphalian_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LSA }
			eastphalia_area = { limit = { owned_by = ROOT } remove_core = LSA add_core = LSA }
			eastphalia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LSA }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			if = {
				limit = { NOT = { hre_size = 1 } government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = LSA
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_sardinia_piedmont = {
		major = yes
		potential = {
			NOT = { exists = SPI }
			NOT = { exists = ITA }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			culture_group = latin
			capital_scope = { OR = { area = piedmont_area area = liguria_area area = corsica_sardinia_area } }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			OR = {
				adm_tech = 35
				AND = {
					NOT = { absolutism = 80 }
					stability = 1
					full_idea_group = diplomatic_ideas
				}
				AND = {
					adm_tech = 19
					ADM = 5
					statesman = 1
				}
			}
			owns = 103 # Piemonte
			OR = {
				AND = {
					check_variable = { which = "Demesne_in_Liguria_Piedmont" value = 5 }
					check_variable = { which = "Demesne_in_Sardinia" value = 4 }
				}
				AND = {
					check_variable = { which = "Cores_on_Liguria_Piedmont" value = 4 }
					check_variable = { which = "Cores_on_Sardinia" value = 3 }
				}
			}
			is_at_war = no
		}
		effect = {
			corsica_sardinia_area = { limit = { owned_by = ROOT } remove_core = SPI add_core = SPI }
			corsica_sardinia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPI }
			piedmont_area = { limit = { owned_by = ROOT } remove_core = SPI add_core = SPI }
			piedmont_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPI }
			liguria_area = { limit = { owned_by = ROOT } remove_core = SPI add_core = SPI }
			liguria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPI }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						SAR = { vassal_of = ROOT }
						SAR = { junior_union_with = ROOT }
					}
				}
				inherit = SAR
			}
			change_tag = SPI
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = SPI_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_siam = {
		major = yes
		potential = {
			NOT = { tag = SUV }
			NOT = { exists = SIA }
			#num_of_cities = 16
			culture_group = thai_group
			is_year = 1700
			is_colonial_nation = no
		}
		allow = {
			OR = {
				AND = {
					check_variable = { which = "Demesne_in_Thai_Plain" value = 10 }
					check_variable = { which = "Demesne_in_Lan_Xang" value = 9 }
				}
				AND = {
					check_variable = { which = "Cores_on_Thai_Plain" value = 8 }
					check_variable = { which = "Cores_on_Lan_Xang" value = 7 }
				}
			}
			is_free_or_tributary_trigger = yes
			owns = 600  #Ayutthaya
			owns = 601  #Sukhotai
			owns = 602  #Lopburi
			is_at_war = no
		}
		effect = {
			lan_xang_region = { limit = { owned_by = ROOT } remove_core = SIA add_core = SIA }
			lan_xang_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SIA }
			inland_siam_region = { limit = { owned_by = ROOT } remove_core = SIA add_core = SIA }
			inland_siam_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SIA }
			coastal_siam_region = { limit = { owned_by = ROOT } remove_core = SIA add_core = SIA }
			coastal_siam_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SIA }
			add_prestige = 20
			change_tag = SIA
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = AYU_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_goldenhorde = {
		major = yes
		potential = {
			NOT = { exists = GOL }
			OR = {
				tag = BLU
				tag = WHI
				tag = CHU
			}
		}
		allow = {
			OR = {
				AND = {
					tag = BLU
					NOT = { exists = WHI }
					NOT = { exists = CHU }
				}
				AND = {
					tag = WHI
					NOT = { exists = BLU }
					NOT = { exists = CHU }
				}
				AND = {
					tag = CHU
					NOT = { exists = BLU }
					NOT = { exists = WHI }
				}
			}
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_Altaic_Throne" value = 28 }
				check_variable = { which = "Cores_on_Altaic_Throne" value = 21 }
			}
			owns = 1293	# Astrakhan
			owns = 1310 # Sarai-al-Jadid
			owns = 2444 # Sarai Juk
			is_at_war = no
		}
		effect = {
			steppes_region = { limit = { owned_by = ROOT } remove_core = GOL add_core = GOL }
			steppes_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GOL }
			kazakh_region = { limit = { owned_by = ROOT } remove_core = GOL add_core = GOL }
			kazakh_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GOL }
			add_prestige = 20
			change_tag = GOL
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_celestial_empire = {
		major = yes
		potential = {
			OR = {
				culture_group = chinese_group
				has_country_flag = barbarian_claimant_china
			}
			is_emperor_of_china = no
			is_colonial_nation = no
			NOT = { has_country_flag = mandate_of_heaven_claimed }
		}
		allow = {
			OR = {
				check_variable = { which = "Demesne_in_the_Mandate_of_Heaven" value = 135 }
				check_variable = { which = "Cores_on_the_Mandate_of_Heaven" value = 135 }
			}
			OR = {
				owns = 701 #Kaifeng
				owns = 702 #Luoyang
				owns = 708 #Beijing
				owns = 2150 #Nanjing
				owns = 2252 #Xi'an
			}
			is_at_war = no
		}
		effect = {
			set_country_flag = mandate_of_heaven_claimed
			random_country = {
				limit = {
					has_country_flag = former_dynasty_china
				}
				clr_country_flag = former_dynasty_china
			}
			random_country = {
				limit = {
					has_country_flag = mandate_of_heaven_claimed
					NOT = { tag = ROOT }
				}
				clr_country_flag = mandate_of_heaven_claimed
				set_country_flag = former_dynasty_china
			}
			clr_country_flag = red_turban_reb
			#north_china_superregion = { add_core = ROOT }
			#east_china_superregion = { add_core = ROOT }
			#southwest_china_superregion = { add_core = ROOT }
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_6 } }
				change_title_6 = yes
			}
			add_legitimacy = 100
			add_stability_3 = yes
			add_prestige = 20
			if = {
				limit = {
					culture_group = chinese_group
				}
				north_china_superregion = { limit = { culture_group = chinese_group } remove_core = ROOT add_core = ROOT }
				north_china_superregion = { limit = { NOT = { culture_group = chinese_group } } add_permanent_claim = ROOT }
				east_china_superregion = { limit = { culture_group = chinese_group } remove_core = ROOT add_core = ROOT }
				east_china_superregion = { limit = { NOT = { culture_group = chinese_group } } add_permanent_claim = ROOT }
				southwest_china_superregion = { limit = { culture_group = chinese_group } remove_core = ROOT add_core = ROOT }
				southwest_china_superregion = { limit = { NOT = { culture_group = chinese_group } } add_permanent_claim = ROOT }
			}
			if = {
				limit = {
					has_country_flag = barbarian_claimant_china
				}
				north_china_superregion = { limit = { owned_by = ROOT } remove_core = ROOT add_core = ROOT }
				north_china_superregion = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ROOT }
				east_china_superregion = { limit = { owned_by = ROOT } remove_core = ROOT add_core = ROOT }
				east_china_superregion = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ROOT }
				southwest_china_superregion = { limit = { owned_by = ROOT } remove_core = ROOT add_core = ROOT }
				southwest_china_superregion = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ROOT }
			}
			add_absolutism = 10
			country_event = { id = chinese_factions.1 days = 1 }
			if = {
				limit = {
					NOT = { tag = QNG }
				}
				country_event = { id = red_turban.13 days = 1 }
			}
			if = {
				limit = {
					technology_group = steppestech
				}
				change_technology_group = chinese
				change_unit_type = chinese
			}
			add_country_modifier = { name = "recently_unified_china" duration = 1825 }
			remove_country_modifier = mongol_xingsheng
			remove_country_modifier = non_imperial_hegemon
			set_emperor_of_china = ROOT
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	#westphalia_nation = {
	#	major = yes
	#	potential = {
	#		NOT = { exists = WES }
	#		NOT = { tag = PAP }
	#		NOT = { tag = ERG }
	#		NOT = { tag = GER }
	#		NOT = { tag = USA }
	#		NOT = { tag = BRZ }
	#		NOT = { tag = MEX }
	#		NOT = { tag = LSA }
	#		NOT = { tag = HSA }
	#		OR = {
	#			primary_culture = hessian
	#			primary_culture = old_saxon
	#		}
	#		is_colonial_nation = no
	#	}
	#	allow = {
	#		is_at_war = no
	#		OR = {
	#			adm_tech = 50
	#			NOT = { hre_size = 1 }
	#		}
	#		OR = {
	#			check_variable = { which = "Demesne_in_the_Lower_Rhine_Circuit" value = 5 }
	#			check_variable = { which = "Cores_on_the_Lower_Rhine_Circuit" value = 3 }
	#		}
	#	}
	#	effect = {
	#		westphalian_circle_region = { limit = { owned_by = ROOT } remove_core = WES add_core = WES }
	#		westphalian_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = WES }
	#		add_prestige = 20
	#		if = {
	#			limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
	#			change_title_5 = yes
	#		}
	#		change_tag = WES
	#		add_absolutism = 10
	#	}
	#	ai_will_do = {
	#		factor = 1
	#	}
	#}
	
	malaccan_nation = {
		major = yes
		potential = {
			NOT = { exists = MLC }
			NOT = { tag = MPH }
			NOT = { tag = SRV }
			NOT = { tag = IND }
			primary_culture = malayan
			check_variable = { which = "Demesne_in_Malaya_Peninsula" value = 1 }
			is_colonial_nation = no
		}
		allow = {
			is_at_war = no
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_Malaya_Peninsula" value = 12 }
				check_variable = { which = "Cores_on_Malaya_Peninsula" value = 8 }
			}
			owns = 596 # Selangor
		}
		effect = {
			malaya_region = { limit = { owned_by = ROOT } remove_core = MLC add_core = MLC }
			malaya_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MLC }
			596 = {
				# add_base_tax = 4
				# add_base_production = 4
				add_building = fort_15th
			}
			MLC = { set_capital = 596 }
			add_prestige = 20
			change_tag = MLC
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = MLC_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	polish_nation = {
		major = yes
		potential = {
			NOT = { exists = POL }
			OR = {
				tag = MAZ
				AND = {
					primary_culture = silesian
					dynasty = "Piast"
				}
			}
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		allow = {
			is_at_war = no
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_Poland" value = 18 }
				check_variable = { which = "Cores_on_Poland" value = 12 }
			}
			owns = 256 # Mazowieckie
		}
		effect = {
			polonia_major_region = { limit = { owned_by = ROOT } remove_core = POL add_core = POL }
			polonia_major_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = POL }
			polonia_minor_region = { limit = { owned_by = ROOT } remove_core = POL add_core = POL }
			polonia_minor_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = POL }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = POL
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = POL_ideas }
				}
				swap_national_ideas_effect = yes
			}
			set_country_flag = kingdom_of_poland
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	lithuanian_nation = {
		major = yes
		potential = {
			NOT = { exists = LIT }
			OR = {
				primary_culture = lithuanian
				AND = {
					primary_culture = ruthenian
					dynasty = "Gediminai"
				}
			}
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			NOT = { tag = RUS }
			NOT = { tag = PLC }
			is_colonial_nation = no
		}
		allow = {
			is_at_war = no
			is_free_or_tributary_trigger = yes
			OR = {
				tag = TRK
				AND = {
					check_variable = { which = "Demesne_in_Lithuania" value = 15 }
					owns = 272 # Aukstaitija
				}
				AND = {
					check_variable = { which = "Cores_on_Lithuania" value = 10 }
					owns = 272 # Aukstaitija
				}
			}
		}
		effect = {
			ruthenia_region = { limit = { owned_by = ROOT } remove_core = ROOT add_core = LIT }
			ruthenia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LIT }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			change_tag = LIT
			add_absolutism = 5
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = LIT_ideas }
				}
				swap_national_ideas_effect = yes
			}
			set_country_flag = grand_duke_of_lithuania
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	polish_lithuanian_nation_dip = {
		major = yes
		potential = {
			NOT = { exists = PLC }
			tag = POL
		}
		allow = {
			is_at_war = no
			is_free_or_tributary_trigger = yes
			OR = {
				adm_tech = 20
				AND = {
					ADM = 5
					statesman = 1
					full_idea_group = aristocracy_ideas
				}
			}
			LIT = { is_subject_of = ROOT }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.1" has_country_flag = 1_union_krewo }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.2" has_country_flag = 2_union_horodlo }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.3" has_country_flag = 3_union_grodno }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.4" has_country_flag = 4_union_krakow }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.5" has_country_flag = 5_union_lublin }
		}
		effect = {
			polonia_major_region = { limit = { owned_by = ROOT } remove_core = PLC add_core = PLC }
			polonia_major_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			polonia_minor_region = { limit = { owned_by = ROOT } remove_core = PLC add_core = PLC }
			polonia_minor_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			lithuania_region = { limit = { owned_by = ROOT } remove_core = PLC add_core = PLC }
			lithuania_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			ruthenia_region = { limit = { owned_by = ROOT } remove_core = PLC add_core = PLC }
			ruthenia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						LIT = { vassal_of = ROOT }
						LIT = { junior_union_with = ROOT }
					}
				}
				inherit = LIT
			}
			change_tag = PLC
			add_absolutism = 10
			set_country_flag = kingdom_of_poland
			set_country_flag = grand_duke_of_lithuania
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = POL_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	polish_lithuanian_nation = {
		major = yes
		potential = {
			NOT = { exists = PLC }
			tag = POL
		}
		allow = {
			is_at_war = no
			is_free_or_tributary_trigger = yes
			OR = {
				adm_tech = 20
				AND = {
					ADM = 5
					statesman = 1
					full_idea_group = aristocracy_ideas
				}
			}
			NOT = { exists = LIT }
			OR = {
				check_variable = { which = "Demesne_in_Lithuania" value = 30 }
				check_variable = { which = "Cores_on_Lithuania" value = 20 }
			}
		}
		effect = {
			polonia_major_region = { limit = { owned_by = ROOT } remove_core = PLC add_core = PLC }
			polonia_major_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			polonia_minor_region = { limit = { owned_by = ROOT } remove_core = PLC add_core = PLC }
			polonia_minor_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			lithuania_region = { limit = { owned_by = ROOT } remove_core = PLC add_core = PLC }
			lithuania_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			ruthenia_region = { limit = { owned_by = ROOT } remove_core = PLC add_core = PLC }
			ruthenia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						LIT = { vassal_of = ROOT }
						LIT = { junior_union_with = ROOT }
					}
				}
				inherit = LIT
			}
			change_tag = PLC
			add_absolutism = 10
			set_country_flag = kingdom_of_poland
			set_country_flag = grand_duke_of_lithuania
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = POL_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	austrian_nation = {
		major = yes
		potential = {
			NOT = { exists = HAB }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			OR = {
				primary_culture = austrian
				primary_culture = slovenian
			}
			government = monarchy
		}
		allow = {
			is_at_war = no
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_Austrian_Circuit" value = 5 }
				check_variable = { which = "Cores_on_Austrian_Circuit" value = 3 }
				owns = 83 # Ostmark
			}
		}
		effect = {
			austrian_circle_region = { limit = { owned_by = ROOT } remove_core = HAB add_core = HAB }
			austrian_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = HAB }
			add_prestige = 10
			change_tag = HAB
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = HAB_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	egyptian_nation = {
		major = yes
		potential = {
			tag = MAM
			NOT = {
				exists = EGY
			}
		}
		allow = {
			has_institution = enlightenment
			is_at_war = no
			is_free_or_tributary_trigger = yes
			egypt_region = { owned_by = ROOT }
		}
		effect = {
			egypt_region = { limit = { owned_by = ROOT } remove_core = EGY add_core = EGY }
			egypt_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = EGY }
			add_prestige = 10
			change_tag = EGY
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	jurchen_nation = {
		major = yes
		potential = {
			NOT = { exists = MCH }
			NOT = { tag = QNG }
			primary_culture = jurchen
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {
				num_of_owned_provinces_with = {
					value = 14
					culture = jurchen
				}
				tooltip = jurchen_nation_claim
			}
			custom_trigger_tooltip = {
				OR = {
					tag = MHX
					NOT = {
						exists = MHX
					}
					MHX = {
						vassal_of = ROOT
					}
					MHX = {
						march_of = ROOT
					}
				}
				OR = {
					tag = MJZ
					NOT = {
						exists = MJZ
					}
					MJZ = {
						vassal_of = ROOT
					}
					MJZ = {
						march_of = ROOT
					}
				}
				OR = {
					tag = MHR
					NOT = {
						exists = MHR
					}
					MHR = {
						vassal_of = ROOT
					}
					MHR = {
						march_of = ROOT
					}
				}
				OR = {
					tag = MWK
					NOT = {
						exists = MWK
					}
					MWK = {
						vassal_of = ROOT
					}
					MWK = {
						march_of = ROOT
					}
				}
				OR = {
					tag = MUD
					NOT = {
						exists = MUD
					}
					MUD = {
						vassal_of = ROOT
					}
					MUD = {
						march_of = ROOT
					}
				}
				OR = {
					tag = MRC
					NOT = {
						exists = MRC
					}
					MRC = {
						vassal_of = ROOT
					}
					MRC = {
						march_of = ROOT
					}
				}
				OR = {
					tag = MNA
					NOT = {
						exists = MNA
					}
					MNA = {
						vassal_of = ROOT
					}
					MNA = {
						march_of = ROOT
					}
				}
				tooltip = jurchen_nations
			}
		}
		effect = {
			add_prestige = 20
			add_manpower = 20
			change_tag = MCH
			if = {
				limit = {
					exists = MHX
					NOT = { tag = MHX }
				}
				inherit = MHX
			}
			if = {
				limit = {
					exists = MJZ
					NOT = { tag = MJZ }
				}
				inherit = MJZ
			}
			if = {
				limit = {
					exists = MWK
					NOT = { tag = MWK }
				}
				inherit = MWK
			}
			if = {
				limit = {
					exists = MHR
					NOT = { tag = MHR }
				}
				inherit = MHR
			}
			if = {
				limit = {
					exists = MUD
					NOT = { tag = MUD }
				}
				inherit = MUD
			}
			if = {
				limit = {
					exists = MXI
					NOT = { tag = MXI }
				}
				inherit = MXI
			}
			if = {
				limit = {
					exists = MRC
					NOT = { tag = MRC }
				}
				inherit = MRC
			}
			if = {
				limit = {
					exists = MNA
					NOT = { tag = MNA }
				}
				inherit = MNA
			}
			add_absolutism = 10
			add_adm_power = 1500
			add_dip_power = 1500
			add_mil_power = 1500
			if = {
				limit = {
					government = monarchy
					NOT = {
						has_country_modifier = title_5
						has_country_modifier = title_6
					}
				}
				change_title_5 = yes
			}
			#if = {
			#	limit = { government = tribal_nomads_altaic }
			#	change_government = chinese_monarchy_2
			#}
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = MCH_ideas }
				}
				swap_national_ideas_effect = yes
			}
			east_manchuria_region = { limit = { owned_by = ROOT } remove_core = MCH add_core = MCH }
			east_manchuria_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MCH }
			west_manchuria_region = { limit = { owned_by = ROOT } remove_core = MCH add_core = MCH }
			west_manchuria_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MCH }
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	tibetan_nation = {
		major = yes
		potential = {
			NOT = { exists = TIB }
			is_free_or_tributary_trigger = yes
			OR = {
				primary_culture = tibetan
				primary_culture = bhutanese
				primary_culture = sikkimese
				primary_culture = ladakhi
			}
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {
				num_of_owned_provinces_with = {
					value = 15
					region = tibet_region
				}
				tooltip = tibetan_nation_claim
			}
		}
		effect = {
			add_prestige = 20
			tibet_region = { limit = { owned_by = ROOT } remove_core = TIB add_core = TIB }
			tibet_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = TIB }
			change_tag = TIB
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = TIB_ideas_dg }
				}
				swap_national_ideas_effect = yes
			}
			if = {
				limit = {
					BHU = { vassal_of = ROOT }
				}
				inherit = BHU
			}
			if = {
				limit = {
					MDO = { vassal_of = ROOT }
				}
				inherit = MDO
			}
			if = {
				limit = {
					GUG = { vassal_of = ROOT }
				}
				inherit = GUG
			}
			if = {
				limit = {
					UTS = { vassal_of = ROOT }
				}
				inherit = UTS
			}
			if = {
				limit = {
					LDK = { vassal_of = ROOT }
				}
				inherit = LDK
			}
			if = {
				limit = {
					SKK = { vassal_of = ROOT }
				}
				inherit = SKK
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	pomeranian_nation = {
		major = yes
		potential = {
			NOT = { exists = POM }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			primary_culture = pommeranian
			capital_scope = { or = { area = vorpommern_area area = hinterpommern_area } }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			num_of_owned_provinces_with = {
				value = 4
				or = { area = vorpommern_area area = hinterpommern_area }
			}
			owns = 47 # Vorpommern
			owns = 2843 # Stralsund
			owns = 2844 # Pyritz
			owns = 2847 # Stargard
			is_core = 47 # Vorpommern
			is_core = 2843 # Stralsund
			is_core = 2844 # Pyritz
			is_core = 2847 # Stargard
			is_at_war = no
		}
		effect = {
			vorpommern_area = { limit = { owned_by = ROOT } remove_core = POM add_core = POM }
			vorpommern_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = POM }
			hinterpommern_area = { limit = { owned_by = ROOT } remove_core = POM add_core = POM }
			hinterpommern_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = POM }
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					OR = {
						has_country_modifier = title_1
						has_country_modifier = title_2
					}
				}
				change_title_3 = yes
			}
			change_tag = POM
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = POM_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	become_sultan_of_delhi = {
		major = yes
		potential = {
			NOT = { has_country_flag = ruler_of_the_deccan_flag }
			NOT = { has_country_flag = ruler_of_dravidia_flag }
			OR = {
				NOT = { exists = DLH }
				DLH = { is_subject_of = ROOT }
			}
			NOT = { tag = MUG }
			religion_group = muslim
			capital_scope = {
				indian_region_trigger = yes
			}
			owns_or_non_sovereign_subject_of = 2163		# Delhi
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			OR = {
				check_variable = { which = "Cores_on_India" value = 5 }
				check_variable = { which = "Demesne_in_India" value = 10 }
			}
		}
		effect = {
			inherit = DLH
			add_prestige = 20
			change_tag = DLH
			set_capital = 2163
			change_primary_culture = capital
			remove_country_modifier = blessing_of_god
			every_owned_province = {
				limit = {
					indian_region_trigger = yes
					NOT = { is_core = DLH }
				}
				remove_core = DLH add_core = DLH
			}
			every_owned_province = {
				limit = {
					indian_region_trigger = yes
					NOT = { is_core = DLH }
				}
				add_territorial_core_effect = yes
			}
			if = {
				limit = {
					government = monarchy
					NOT = { has_country_modifier = title_6 }
				}
				change_title_6 = yes
			}
			if = {
				limit = {
					OR = {
						government = rajput_monarchy
						government = tribal_monarchy
						government = tribal_monarchy_elective
						government = tribal_theocracy
						government = tribal_confederation
						government = tribal_federation
					}
				}
				change_government = indian_monarchy
			}
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = DLH_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	georgian_nation = {
		major = yes
		potential = {
			primary_culture = georgian
			NOT = { exists = GEO }
			NOT = { tag = BYZ }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			owns_core_province = 422 # Kakheti
			owns_core_province = 423 # Kartli
			owns_core_province = 1301 # Imeretia
		}
		effect = {
			2549 = { add_core = GEO } # Jiqeti
			1300 = { add_core = GEO } # Abkhazia
			1449 = { add_core = GEO } # Klarjeti
			2581 = { add_core = GEO } # Kars
			419 = { add_core = GEO } # Hayastan
			2576 = { add_core = GEO } # Herethi
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					OR = {
						has_country_modifier = title_1
						has_country_modifier = title_2
						has_country_modifier = title_3
					}
				}
				change_title_4 = yes
			}
			change_tag = GEO
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = GEO_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	assam_nation = {
		major = yes
		potential = {
			culture_group = assamese_group
			NOT = { exists = ASS }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			owns_core_province = 3193 # Sadiya
			owns_core_province = 571 # Brahmaputra
			owns_core_province = 570 # Ahom
		}
		effect = {
			3193 = { add_core = ASS } # Sadiya
			571 = { add_core = ASS } # Brahmaputra
			570 = { add_core = ASS } # Ahom
			566 = { add_core = ASS } # Darrang
			2695 = { add_core = ASS } # Koch Hajo
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					NOT = {
						has_country_modifier = title_5
						has_country_modifier = title_6
					}
				}
				change_title_5 = yes
			}
			change_tag = ASS
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = ASS_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	
	
	form_morocco = { #an alternative to "Fez" for countires that otherwise meet the requirments and want a unification
		potential = {
			OR = {
				primary_culture = tuareg
				primary_culture = fassi
				primary_culture = berber
				primary_culture = andalucian
				tag = GRA
				tag = SOS
				tag = TFL
			}
			NOT = { tag = FEZ }
			NOT = { tag = TLE }
			NOT = { tag = ALG }
			NOT = { tag = ALM }
			religion_group = muslim
			OR = {
				NOT = { exists = MOR }
				MOR = { is_subject_of = ROOT }
			}
		}
		allow = {
			is_at_war = no
			is_free_or_tributary_trigger = yes
			OR = {
				check_variable = { which = "Demesne_in_Morocco" value = 15 }
				check_variable = { which = "Cores_on_Morocco" value = 12 }
			}
			OR = {
				adm_tech = 20
				AND = {
					ADM = 5
					statesman = 1
				}
			}
			owns = 347 # Tachelhit
		}
		effect = {
			inherit = MOR
			fez_area = { limit = { owned_by = ROOT } remove_core = MOR add_core = MOR }
			fez_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			marrekesh_area = { limit = { owned_by = ROOT } remove_core = MOR add_core = MOR }
			marrekesh_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			sus_area = { limit = { owned_by = ROOT } remove_core = MOR add_core = MOR }
			sus_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			tafilalt_area = { limit = { owned_by = ROOT } remove_core = MOR add_core = MOR }
			tafilalt_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			anfa_area = { limit = { owned_by = ROOT } remove_core = MOR add_core = MOR }
			anfa_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			change_tag = MOR
			set_capital = 347
			if = {
				limit = {
					technology_group = soudantech
				}
				change_technology_group = muslim
				change_unit_type = muslim
			}
			if = {
				limit = {
					government = monarchy
					NOT = {
						has_country_modifier = title_5
						has_country_modifier = title_6
					}
				}
				change_title_5 = yes
			}
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = MOR_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	form_julich_berg_kleve = {
		major = yes
		potential = {
			OR = {
				tag = JUL
				tag = BRG
				tag = KLE
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			owns = 51 # Kleve
			owns = 1253 # Julich
			owns = 2623 # Berg
		}
		effect = {
			51 = { add_core = JBC } # Kleve
			1253 = { add_core = JBC } # Julich
			2623 = { add_core = JBC } # Berg
			2637 = { add_permanent_claim = JBC } # Mark
			add_prestige = 20
			change_tag = JBC
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = JBC_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	become_brandenburg = {
		major = yes
		potential = {
			OR = {
				tag = HOH
				tag = ANS
				tag = BYR
			}
		}
		allow = {
			is_at_war = no
			NOT = { exists = BRA }
			owns = 50 # Brandenburg
		}
		effect = {
			50 = { add_core = BRA } # Brandenburg
			66 = { add_core = BRA } # Altmark
			264 = { add_core = BRA } # Neumark
			2621 = { add_core = BRA } # Prignitz
			2835 = { add_core = BRA } # Lubusz
			4015 = { add_core = BRA } # Havelland
			set_capital = 50
			add_prestige = 20
			change_tag = BRA
			change_primary_culture = capital
			if = {
				limit = {
					OR = {
						has_country_modifier = title_1
						has_country_modifier = title_2
					}
				}
				change_title_3 = yes
			}
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = PRU_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	claim_sardinia = {
		major = yes
		potential = {
			NOT = { exists = SAR }
			NOT = { exists = SPI }
			NOT = { tag = ITA }
			NOT = { tag = ITC }
			NOT = { tag = ITD }
			NOT = { tag = ITE }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = LAT }
			primary_culture = sardinian
			NOT = { is_emperor = yes }
			capital_scope = { area = corsica_sardinia_area }
			check_variable = { which = "Demesne_in_Sardinia" value = 1 }
			is_colonial_nation = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			government = monarchy
			owns = 2242 # Casteddu
			OR = {
				check_variable = { which = "Demesne_in_Sardinia" value = 4 }
				check_variable = { which = "Cores_on_Sardinia" value = 3 }
			}
		}
		effect = {
			corsica_sardinia_area = { limit = { owned_by = ROOT } remove_core = SAR add_core = SAR }
			corsica_sardinia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SAR }
			add_prestige = 15
			if = {
				limit = {
					government = monarchy
					OR = {
						has_country_modifier = title_1
						has_country_modifier = title_2
						has_country_modifier = title_3
					}
				}
				change_title_4 = yes
			}
			change_tag = SAR
			set_capital = 2242
			add_absolutism = 10
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = SAR_ideas }
				}
				swap_national_ideas_effect = yes
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_ava = {
		major = yes
		potential = {
			NOT = { exists = AVA }
			OR = {
				tag = SAG
				tag = PIN
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			owns_core_province = 584 # Ava
			owns_core_province = 2429 # Sagaing
			OR = {
				AND = {
					tag = SAG
					NOT = { exists = PIN }
				}
				AND = {
					tag = PIN
					NOT = { exists = SAG }
				}
			}
		}
		effect = {
			every_province = {
				limit = {
					OR = {
						region = inland_burma_region
						region = coastal_burma_region
					}
					is_core = ROOT
				}
				add_core = AVA
			}
			add_prestige = 20
			change_tag = AVA
			add_absolutism = 10
			set_capital = 584
			584 = { rename_capital = "Awa" }
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
}
